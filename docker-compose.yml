version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./docs:/app/docs
      - ./vectorstore:/app/vectorstore
    restart: unless-stopped